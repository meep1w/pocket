[Unit]
Description=PocketBot - HTTP App
After=network.target

[Service]
WorkingDirectory=/opt/pocketbot
Environment="PYTHONPATH=/opt/pocketbot"
ExecStart=/usr/bin/uvicorn app.http.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
